# ═══════════════════════════════════════════════════════════════════
# requirements.txt - منصة SAIF.AI
# ═══════════════════════════════════════════════════════════════════

# ── الإطار الأساسي (Core Framework) ──────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# ── قاعدة البيانات (Database) ────────────────
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.1
psycopg2-binary==2.9.10

# ── التخزين المؤقت (Cache) ──────────────────
redis==5.2.1
aioredis==2.0.1

# ── الإعدادات والبيئة (Settings & Env) ──────
pydantic==2.10.4
pydantic-settings==2.6.1
python-dotenv==1.0.1

# ── HTTP Client ──────────────────────────────
httpx==0.28.1
aiohttp==3.11.10
requests==2.32.3

# ── معالجة الملفات (File Processing) ────────
aiofiles==24.1.0
python-magic==0.4.27
PyPDF2==3.0.1
python-docx==1.1.2
pillow==11.0.0

# ── الذكاء الاصطناعي (AI/LLM) ───────────────
openai==1.59.3
google-generativeai==0.8.3

# ── الصوت (Audio) ────────────────────────────
pydub==0.25.1
SpeechRecognition==3.10.4

# ── الأمان (Security) ────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── أدوات إضافية (Utilities) ─────────────────
tenacity==9.0.0
click==8.1.7
colorama==0.4.6
tqdm==4.67.0
rich==13.9.4
loguru==0.7.3

# ── التحليل والإحصائيات (Analytics) ─────────
pandas==2.2.3
numpy==2.2.0

# ── التطوير والاختبار (Development) ─────────
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.14.1

# ── التوثيق (Documentation) ──────────────────
markdown==3.7

# ── التنسيق (Formatting) ────────────────────
email-validator==2.2.0
jinja2==3.1.4

# ── معالجة JSON ─────────────────────────────
orjson==3.10.11

# ── WebSocket ────────────────────────────────
websockets==14.1

# ── التوقيت (Scheduling) ────────────────────
schedule==1.2.2

# ── أدوات النظام (System) ───────────────────
psutil==6.1.0

# ── معالجة البيانات النصية (Text Processing) ─
chardet==5.2.0
unidecode==1.3.8